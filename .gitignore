# Artefactos de PyInstaller (se regeneran con `python -m PyInstaller app/GUI_Watermark.spec`)
build/
dist/

# Bytecode de Python
__pycache__/
*.pyc

# Salida de graphify (grafo generado, no es código fuente del proyecto)
graphify-out/

# Nota: admin_tools/private_key.pem y admin_tools/*.exe se incluyen a
# propósito en el repo por decisión explícita (01/09/2026), pese a la
# recomendación original de mantener la clave privada fuera del repo.
# Ver admin_tools/generate_keypair.py.
admin_tools/build/
admin_tools/dist/
